#! /bin/bash
# /usr/local/lib/zconnect/mail.zconnect
#
host="$2"
dir=/var/spool/zconnect/z-outq/$host
if [ ! -d $dir ]; then mkdir $dir; fi
mailfile="$dir/q`date +%Y%m%d%H%M%S`"
(
echo "MAIL FROM: <$3>"
echo "RCPT TO: <$1>"
echo "DATA"
cat
echo "."
) > $mailfile
